# V10 Driver Locked UI

Fixed:
- Driver role no longer sees profit dashboard
- Driver role no longer sees office/admin tabs
- Driver role only sees Driver App
- Bottom nav simplified for driver
- Backend blocks /api/dashboard for driver role

Admin/office roles still see full ERP.

Create driver user with:
role = driver

Deploy:
Upload all files to GitHub repo root and redeploy Railway.
